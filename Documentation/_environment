# Use the project virtualenv (which has BayesForge + the Jupyter stack:
# nbformat, nbclient, jupyter-client, ipykernel) to execute {python} cells.
# The system /usr/bin/python3 has no Jupyter, which makes executable docs
# fail to start a kernel ("No module named 'nbformat'") during render.
QUARTO_PYTHON=/home/sebastian_sosa/venv/bin/python
