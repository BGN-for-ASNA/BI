bi.dist.asymmetriclaplacequantile=function(loc=0.0, scale=1.0, quantile=0.5, validate_args=py_none(), shape=c(), sample=FALSE, seed=0, name='x') {    shape=do.call(tuple, as.list(as.integer(shape)));    seed=as.integer(seed);    bi$bi$dist$asymmetriclaplacequantile(loc=loc,  scale= scale,  quantile= quantile,  validate_args= validate_args,  shape= shape,  sample= sample,  seed= seed,  name= name)}
