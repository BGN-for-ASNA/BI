bi.dist.geometricprobs=function(probs, validate_args=py_none(), shape=c(), sample=FALSE, seed=0, name='x') {    shape=do.call(tuple, as.list(as.integer(shape)));    seed=as.integer(seed);    bi$bi$dist$geometricprobs(probs,  validate_args= validate_args,  shape= shape,  sample= sample,  seed= seed,  name= name)}
